{
  "config": {
    "step": {
      "user": {
        "title": "MQTT Media Player"
      },
      "mqtt": {
        "title": "MQTT Media Player"
      }
    },
    "abort": {
      "mqtt_discovery_only": "This integration is discovery-only. Add `mqtt_media_player:` to configuration.yaml and restart Home Assistant, then keep a player online and publishing to MQTT: it will be discovered automatically."
    }
  }
}
