{
  "config": {
    "step": {
      "user": {
        "title": "MQTT Media Player",
        "description": "Set this up once. It watches the MQTT discovery topic, so every player that comes online and publishes to MQTT is then added automatically. No configuration.yaml change is needed."
      },
      "mqtt": {
        "title": "MQTT Media Player"
      }
    }
  }
}
