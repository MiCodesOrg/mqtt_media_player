{
  "config": {
    "step": {
      "user": {
        "title": "MQTT Media Player"
      },
      "mqtt": {
        "title": "MQTT Media Player"
      }
    },
    "abort": {
      "mqtt_discovery_only": "MQTT Media Players are automatically discovered when online and publishing to MQTT."
    }
  }
}
